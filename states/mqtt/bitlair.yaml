name: bitlair
index: 99
check: states['bitlair']
delay: 30
command: >
  while true; do
    convert -background black -fill orange -font Noto-Mono -pointsize 14 -size 96x32 -gravity center -depth 8 caption:"Bitlair" RGB:-
    sleep 1;
  done