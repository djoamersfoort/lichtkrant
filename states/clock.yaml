name: clock
index: 0 # fallback
check: false
command: >
  while true; do
    convert -background black -fill green -font Noto-Mono -pointsize 20 -size 96x32 -gravity center -depth 8 caption:"$(date +%T)" RGB:-
    sleep 0.8;
  done
